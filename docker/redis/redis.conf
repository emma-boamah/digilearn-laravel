# Set Redis to master node
replicaof no one

# Allow writes to this instance
replica-read-only no

# Disable protected mode for Docker networking
protected-mode no

# Optional: Set maxmemory policy
maxmemory 500mb
maxmemory-policy allkeys-lru
